
project.ext {

    // Required: inventory file
    inventory = ""

    // Optional: base directory for .tsv or .csv files. Not needed
    // if using only indices stored in Google Fusion Tables
    baseDirectory = ""



    // Settings for concordion:
    mdSrc = "specs/md"
    
    htmlPreface = """
<html xmlns:concordion="http://www.concordion.org/2007/concordion">
<body>
    """
    htmlEnd = "</body>\n</html>\n"

}
